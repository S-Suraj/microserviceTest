version: '2'
services:
    microtest-postgresql:
        container_name: microtest-postgresql
        image: postgres:9.5.4
        # volumes:
        #     - ~/volumes/jhipster/microtest/postgresql/:/var/lib/postgresql/
        environment:
            - POSTGRES_USER=microtest
            - POSTGRES_PASSWORD=
        ports:
            - 5432:5432
